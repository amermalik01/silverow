// app/components/sales/crm/GeneralSection.tsx

/* export default function GeneralSection({ form, setForm }: any) {
  return (
    <div className="bg-white p-4 rounded shadow space-y-4">

      <h2 className="font-semibold text-lg">General</h2>

      <input
        placeholder="CRM Code"
        value={form.crm_code}
        onChange={(e) => setForm({ ...form, crm_code: e.target.value })}
        className="border p-2 w-full rounded"
      />

      <input
        placeholder="Name"
        value={form.name}
        onChange={(e) => setForm({ ...form, name: e.target.value })}
        className="border p-2 w-full rounded"
      />

      <input
        placeholder="Email"
        value={form.email}
        onChange={(e) => setForm({ ...form, email: e.target.value })}
        className="border p-2 w-full rounded"
      />

    </div>
  );
} */